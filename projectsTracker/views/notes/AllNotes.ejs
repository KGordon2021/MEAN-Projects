<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="public/styles.css">
    <title>Document</title>
</head>
<body>
    <div class="pages">
        <%- include('../partials/nav.ejs') %>
    </div>
    <div class="tables table2">
    <table>
        <thead>
            <tr>
                <th>Notes Id #</th>
                <th>Notes Description</th>
                <th>Active Date</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            
            <% if(notes.length) {
                for(var i = 0; i < notes.length; i++) { 
            %>
            <tr>
                <td class="centered"><%= notes[i].project_id %></td>
                <td class="brk"><%= notes[i].note %></td>
                <td><%= new Date(notes[i].active_date).toLocaleDateString() %></td>
                <td class="centered">
                    <a href="/editNotes/update/<%=notes[i].project_id%>">Update</a> <!--this refers to the index of the current notes collection which is currently stored as notes-->
                    <a href="/allNotes/delete/<%=notes[i].project_id%>" style="margin-right: 10px;">Remove</a>
                </td>
                
            </tr>
               <% }
          }else{ %>
            <tr>
                <td>No students</td>
            </tr>
           <% } %>
        </tbody>
        
    </table>
    </div>
</body>
</html>